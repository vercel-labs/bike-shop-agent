# The `eve` CLI binary (`eve dev`) ships in the 0.6.0-beta line, which matches
# the current docs. Stable 0.5.4 has no bin. Allow the freshly-published beta
# past the global minimumReleaseAge guard for this package only.
minimum-release-age-exclude[]=eve
